<!DOCTYPE html>
<html>
<head>
  <title>nuage de son</title>
  <%= stylesheet_link_tag    "application", :media => "all" %>
  <%= javascript_include_tag "application" %>
  <%= csrf_meta_tags %>
</head>
<body>

  <div id="main">Loading...</div>

</body>
<script type="text/javascript">
    <% if signed_in? %>
     (function(){
        Nuagedeson.initialize(<%= current_user.to_json.html_safe %>);
        if (window.location.pathname === "/_=_") {
         Nuagedeson.mainRouter.navigate('users', {trigger: true});
        }
        // Nuagedeson.mainRouter.navigate('clips', {trigger: true});
      })();
    <% else %>
      Nuagedeson.initialize();
    <% end %>

  </script>
</html>
